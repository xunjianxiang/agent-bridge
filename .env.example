HOST=127.0.0.1
PORT=8787
LOG_LEVEL=info
PROVIDER_DETECTION_TTL_MS=30000
# Browser CORS is disabled by default. Set explicit origins only when needed.
# CORS_ORIGINS=http://localhost:3000,https://example.com
